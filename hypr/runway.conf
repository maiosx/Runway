# Runway — fullscreen checking forecast
# Super + R summons / dismisses the overlay.
# If SUPER+R is already bound (stock Hyprland resize submap), comment that bind out first.

bindd = SUPER, R, Toggle Runway, exec, omarchy-shell shell toggle runway.forecast
